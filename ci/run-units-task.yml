---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/golang

inputs:
- name: bbl-state-resource-src

run:
  path: bbl-state-resource-src/ci/run-tests
  args: [-skipPackage acceptance_test]
